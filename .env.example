# Demo mode is used when these are blank.
JELLYFIN_URL=http://jellyfin:8096
JELLYFIN_API_KEY=
JELLYFIN_USER_ID=

# Public browser-facing Jellyfin URL on your home network.
# Example: http://192.168.50.20:8096
NEXT_PUBLIC_JELLYFIN_URL=http://localhost:8096

# Server-side PostgreSQL 18 connection (reelhouse database), least-privilege
# application role. Blank = no database features (demo mode). Credentials
# stay server-side; they are never sent to the browser. See docs/DATABASE.md.
# Example: postgresql://reelhouse_app:CHANGE_ME@192.168.50.20:5432/reelhouse
DATABASE_URL=

# Owner/migrator role used only by `npm run db:migrate`. Falls back to
# DATABASE_URL when unset. Never exposed to the application runtime.
# Example: postgresql://reelhouse_owner:CHANGE_ME@192.168.50.20:5432/reelhouse
DATABASE_MIGRATE_URL=

# Explicit application role name for migration grants ({{app_role}}).
# Defaults to the user in DATABASE_URL. See docs/DATABASE.md.
DATABASE_APP_ROLE=

# Media catalog database (RH-0026), SEPARATE from the reelhouse household
# database. Required by `npm run catalog:sync`; blank = catalog features off.
# Example: postgresql://reelhouse_app:CHANGE_ME@192.168.50.20:5432/media_catalog
MEDIA_CATALOG_DATABASE_URL=

# Catalog owner/migrator role used only by `npm run catalog:migrate`. Falls
# back to MEDIA_CATALOG_DATABASE_URL when unset.
# Example: postgresql://reelhouse_owner:CHANGE_ME@192.168.50.20:5432/media_catalog
MEDIA_CATALOG_MIGRATE_URL=

# Explicit catalog sync role name for migration grants ({{app_role}}).
# Defaults to the user in MEDIA_CATALOG_DATABASE_URL.
MEDIA_CATALOG_APP_ROLE=

# Jellyfin sync tuning: per-request timeout (1000-120000, default 30000) and
# the retirement threshold for items absent from full scans (1-3650 days,
# default 30). See docs/CATALOG_SYNC.md.
# JELLYFIN_SYNC_TIMEOUT_MS=30000
# MEDIA_CATALOG_RETIREMENT_DAYS=30

# Optional bounded tuning overrides (defaults in docs/DATABASE.md):
# DATABASE_POOL_MAX=10
# DATABASE_CONNECT_TIMEOUT_MS=10000
# DATABASE_IDLE_TIMEOUT_MS=30000
# DATABASE_STATEMENT_TIMEOUT_MS=10000
# JELLYFIN_HEALTH_TIMEOUT_MS=3000
